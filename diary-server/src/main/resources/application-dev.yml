spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:diary_dev}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:}
  jpa:
    show-sql: true

logging:
  level:
    com.diary.server: DEBUG
