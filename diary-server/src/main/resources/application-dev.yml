spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:diary_dev}
    username: ${DB_USER:diary}
    password: ${DB_PASSWORD:diary}
  jpa:
    show-sql: true

logging:
  level:
    com.diary.server: DEBUG
